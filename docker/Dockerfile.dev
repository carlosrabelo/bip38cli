# Development Dockerfile with hot reload capabilities
FROM golang:1.24-alpine

# Install development dependencies
RUN apk add --no-cache git ca-certificates tzdata air

# Set working directory
WORKDIR /app

# Copy go mod files
COPY core/go.mod core/go.sum ./

# Download dependencies
RUN go mod download

# Copy source code
COPY core/ .

# Install air for hot reload (if not already available)
RUN go install github.com/cosmtrek/air@latest

# Expose port for potential web interface (future)
EXPOSE 8080

# Default command runs the application with hot reload
CMD ["air", "-c", ".air.toml"]
